<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">

    <item
        android:id="@+id/main_menu_attention"
        android:icon="@mipmap/profile_day_icon"
        android:title="@string/main_attention"
        app:showAsAction="always"
        />
    <item
        android:id="@+id/main_menu_add"
        android:icon="@mipmap/btn_plus"
        android:title="@string/main_add"
        app:showAsAction="always"
        />
    <item
        android:id="@+id/main_menu_search"
        android:icon="@mipmap/reader_topbar_search"
        android:title="@string/main_search"
        app:showAsAction="always"
        />
</menu>
